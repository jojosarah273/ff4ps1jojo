/* FF4 source-port — interpreted module for func_8014076C.
 * Ground truth: src/func_8014076C.c (byte-verified).
 * Primitives: port/include/ff4_window.h.
 */
#include "ff4_window.h"

void func_8014076C(void)
{
        if (poll_go(514)) {
        if (poll_go(514)) {
    } else {
    }

    } else {
    }

}
