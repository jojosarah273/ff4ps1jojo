/* FF4 source-port — interpreted module for func_80116348.
 * Ground truth: src/func_80116348.c (byte-verified).
 * Primitives: port/include/ff4_window.h.
 */
#include "ff4_window.h"

void func_80116348(void)
{
        if (io_just()) {
        if (io_just()) {
    } else {
    }

    } else {
    }

}
