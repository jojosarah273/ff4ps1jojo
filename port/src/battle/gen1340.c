/* FF4 source-port — interpreted module for midrow_degen.
 * Ground truth: src/midrow_degen.c (byte-verified).
 * Primitives: port/include/ff4_window.h.
 */
#include "ff4_window.h"

void midrow_degen(void)
{
        if (ticker_reblend2(2)) {
        if (ticker_reblend2(2)) {
    } else {
    }

    } else {
        if (ticker_reblend2(2)) {
    } else {
    }

    }

}
