/* FF4 source-port — interpreted module for fn_1x183d8.
 * Ground truth: src/fn_1x183d8.c (byte-verified).
 * Primitives: port/include/ff4_window.h.
 */
#include "ff4_window.h"

void fn_1x183d8(void)
{
        if (io_just()) {
        if (io_just()) {
    } else {
    }

    } else {
        if (io_just()) {
    } else {
    }

    }

}
