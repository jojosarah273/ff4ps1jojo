/* FF4 source-port — interpreted module for fn_1x29838.
 * Ground truth: src/fn_1x29838.c (byte-verified).
 * Primitives: port/include/ff4_window.h.
 */
#include "ff4_window.h"

void fn_1x29838(void)
{
        if (poll_go(514)) {
        if (poll_go(514)) {
    } else {
    }

    } else {
    }

}
