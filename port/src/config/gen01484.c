/* FF4 source-port — interpreted module for fn_1x26920.
 * Ground truth: src/fn_1x26920.c (byte-verified).
 * Primitives: port/include/ff4_window.h.
 */
#include "ff4_window.h"

void fn_1x26920(void)
{
        if (poll_go(514)) {
        if (poll_go(514)) {
    } else {
    }

    } else {
    }

}
