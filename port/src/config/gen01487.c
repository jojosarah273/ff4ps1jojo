/* FF4 source-port — interpreted module for fn_1x2468c.
 * Ground truth: src/fn_1x2468c.c (byte-verified).
 * Primitives: port/include/ff4_window.h.
 */
#include "ff4_window.h"

void fn_1x2468c(void)
{
        if (poll_go(514)) {
        if (poll_go(514)) {
    } else {
    }

    } else {
    }

}
