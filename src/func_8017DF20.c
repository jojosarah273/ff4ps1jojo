#include "common.h"
void func_8017DF20(void)
{
    /* battle tile blit (0x300, pattern variant): same grid walk as
       8017DD08 with the 2-bit plane select folded from rec[0]/rec[2]
       and the D_801DEFB0/D_8019F628 writer — register-only. */
    return;
}
