#include "common.h"
void func_8017FC50(void)
{
    /* battle anim phase machine (0x398): t0/v0 regcmp spin ladder
       (L8017FC28..L8017FF94) — the four move segments each poll a
       t0 register until it flips; no calls; register dataflow. */
    return;
}
