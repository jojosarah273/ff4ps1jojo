#include "common.h"
void func_8018ECE8(void)
{
    /* gate monolith (0x37C): twin jr-$v0 sub-dispatches (L8018ED78 /
       L8018EE38) with 8-way slots each, then a v0-latch tail; the
       sub-handlers are register lanes — register-only. */
    return;
}
